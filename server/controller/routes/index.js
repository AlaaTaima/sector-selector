/** @format */

const { getUserData } = require('./getUserData');
const { addSelectors } = require('./addSelectors');
const { updateSelectors } = require('./updateSelectors');
const { getSectors } = require('./getSectors');

module.exports = {
	getSectors,
	getUserData,
	addSelectors,
	updateSelectors,
};
